# Profile CRUD API

## Запуск

```bash
yarn
docker compose up -d
yarn db:deploy
yarn db:generate
yarn start:dev
```

Создайте .env по примеру .env.example.

После `yarn start:dev` Swagger доступен по адресу: http://localhost:3000/docs

Запуск тестов

```
yarn test
```
