generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           String @unique
  login        String
  email        String
  password     String
  age          Int
  description  String
}
