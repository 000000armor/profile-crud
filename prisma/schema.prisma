generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           String @unique
  login        String @unique
  email        String @unique
  password     String
  age          Int
  description  String
}
